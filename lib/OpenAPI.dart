import 'package:flutter/material.dart';

class OpenAPITestScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(), body: Container(child: Text('Hello there!')));
  }
}
